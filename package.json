{
  "name": "pp-tefeta",
  "version": "1.0.0",
  "main": "maze.js",
  "repository": "https://github.com/MestrieEsteban/pp-tefeta.git",
  "author": "",
  "license": "MIT",
  "scripts": {
    "start": "ts-node src/script.ts",
    "build": "tsc",
    "docker": "yarn docker-build && yarn docker-run",
    "docker-build": "docker build -t tefeta -f Dockerfile .",
    "docker-run": "docker run --rm -it tefeta"
  },
  "dependencies": {
    "@types/node": "^13.13.5",
    "ts-node": "^8.10.1",
    "typescript": "^3.8.3"
  }
}
